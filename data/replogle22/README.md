# Replogle22

This directory contains the data for the Replogle22 dataset.

To get the data, run:

```bash
python data/replogle22/get_data.py
```

This will download the data, do basic preprocessing, and save the data in the `data/replogle22` directory along with a downloaded copy of the data.

